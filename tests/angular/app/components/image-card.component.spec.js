ngDescribe({
    name: 'Test image-card component',
    modules: 'app',
    element: '<image-card></image-card>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
